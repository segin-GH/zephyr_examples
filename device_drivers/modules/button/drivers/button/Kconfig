# Create a new option in menuconfig
config SIMPLE_BUTTON
    bool "Simple button"
    default n       # Set the driver to be disabled by default
    depends on GPIO # Make it dependent on GPIO driver
    help
        Enable the custom button driver.
